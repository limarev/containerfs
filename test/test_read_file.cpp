#include <gtest/gtest.h>
#include <vector>
// #include "ole2/filesystem_view.hpp"

// stub функция для получения эталонного файла через libgsf
std::vector<std::byte> create_test_file_with_libgsf(const std::string &path) {
  // используем libgsf API для создания OLE2 файла с потоком "Sheet1"
  // возвращаем путь к файлу или массив байт для mem_device
}
